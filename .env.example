# Copy to .env and fill in real values. Never commit .env.

# --- Backend (apps/api) ---
ENVIRONMENT=development
# Port 5442: see docker-compose.yml's postgres service for why.
DATABASE_URL=postgresql+psycopg://macenplast:macenplast@localhost:5442/macenplast
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ROUTING_STRATEGY=serpentine

# --- Postgres (docker-compose) ---
POSTGRES_USER=macenplast
POSTGRES_PASSWORD=macenplast
POSTGRES_DB=macenplast

# --- Frontend (apps/web) ---
VITE_API_BASE_URL=http://localhost:8000
